# Production profile - Railway will use this
# Database config will be set via Railway environment variables

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# JWT
jwt.expiration=3600000
jwt.issuer=forma-app

# Stripe (set via env vars in Railway)
# stripe.api.publishable-key=${STRIPE_PUBLISHABLE_KEY}
# stripe.api.secret-key=${STRIPE_SECRET_KEY}
# stripe.api.webhook-secret=${STRIPE_WEBHOOK_SECRET}
